name: Site checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  links-and-reachability:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check static links and class-page reachability
        run: python3 scripts/check_site.py
